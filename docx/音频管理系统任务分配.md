# 任务分配

---

## 1. 于子桐 (kaifeng) - 全栈开发 & 前端主导

### 核心职责概述
负责系统安全认证、前端界面开发、用户管理功能以及注册审核系统的完整实现，是项目的全栈核心开发者。

---

### 一、安全与认证模块

#### JWT 工具类开发 ([JwtUtil.java](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\util\JwtUtil.java))
- Token 生成与解析逻辑实现
- 密钥外部化配置（从 [application.yml](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\target\classes\application.yml) 读取）
- 支持用户名和角色等级信息嵌入
- 24小时过期时间配置

#### JWT 拦截器实现 ([JwtInterceptor.java](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\interceptor\JwtInterceptor.java))
- HTTP 请求拦截与 Token 验证
- 角色信息提取并注入 Request 属性
- 未授权请求的 401 响应处理
- `@NonNull` 注解适配 Spring Boot 3

#### Web 配置类 ([WebConfig.java](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\config\WebConfig.java))
- 注册 JWT 拦截器到 Spring MVC 拦截链
- 配置排除路径：登录接口、注册接口、音频流接口
- 跨域支持配置

#### 认证控制器 ([AuthController.java](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\controller\AuthController.java))
- **用户登录接口** (`POST /api/auth/login`)
    - 用户名密码验证
    - JWT Token 生成与返回
    - 角色等级信息返回
    
- **用户注册功能** (`POST /api/auth/register`)
    - 用户名唯一性校验
    - 待审核状态检查
    - 创建注册申请记录
    - 返回审核提示
    
- **用户管理 CRUD**
    - `GET /api/auth/users` - 获取用户列表（仅管理员）
    - `POST /api/auth/users` - 添加新用户（仅管理员）
    - `PUT /api/auth/users/{id}` - 修改用户信息（仅管理员）
    - `DELETE /api/auth/users/{id}` - 删除用户（仅管理员）
    
- **注册审核功能**
    - `GET /api/auth/registrations` - 获取待审核列表（分页、仅管理员）
    - `POST /api/auth/registrations/{id}/approve` - 审核通过
        - 权限验证
        - 并发冲突检测
        - 创建新用户账号
        - 记录审核人信息和时间
    - `POST /api/auth/registrations/{id}/reject` - 审核拒绝
        - 更新申请状态
        - 记录拒绝原因
        - 记录审核人信息
    
- **权限控制验证**
    - 所有敏感操作的 roleLevel 检查
    - 统一的 403 权限不足响应
    - 详细的操作日志输出

#### 密码加密工具类 ([PasswordUtil.java](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\util\PasswordUtil.java))
- SHA-256 哈希算法实现
- 密码加密方法
- 密码验证方法
- Base64 编码存储

---

### 二、前端开发

#### 完整前端界面 ([index.html](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\resources\static\index.html))

**现代化 UI 设计**
- 侧边栏导航布局（渐变背景、图标菜单）
- 卡片式设计风格
- 渐变按钮与交互动画
- 响应式布局适配

**登录页面**
- 用户名密码输入表单
- 登录验证与错误提示
- 注册入口切换
- LocalStorage Token 管理

**三大功能板块**
1. **音频乐库板块**
    - 音频列表表格展示
    - 分页控件（上一页/下一页）
    - 音频播放器集成
    - 搜索框与关键词过滤
    - 空状态友好提示

2. **上传音频板块**
    - 拖拽上传区域
    - 文件选择按钮
    - 实时上传进度条
    - 文件信息展示（名称、大小、类型）
    - 文件格式和大小验证

3. **用户管理板块**
    - 用户列表展示
    - 角色徽章区分（管理员/操作员）
    - 添加/编辑用户模态框
    - 删除确认对话框

4. **注册申请审核板块**（仅管理员可见）
    - 待审核申请列表
    - 审核模态框（通过/拒绝）
    - 审核备注输入
    - 分页控件

**用户交互功能**
- 拖拽上传支持（dragover/dragleave/drop 事件）
- 上传进度条动态显示（XMLHttpRequest progress 监听）
- 文件信息动态展示
- 平滑的板块切换动画（fadeIn 动画）
- 音频播放错误诊断与提示
- 行内编辑功能（音频名称修改）
- 文件大小格式化显示（B/KB/MB/GB）

**权限控制前端实现**
- 管理员/操作员角色区分显示
- 动态显示/隐藏操作按钮（编辑、删除）
- 侧边栏菜单权限控制（注册审核仅管理员可见）
- 用户管理界面完整 CRUD 操作

**用户体验优化**
- 空状态提示（无数据、无权限、加载失败）
- 操作成功/失败反馈（alert 提示）
- 加载状态指示
- 禁用按钮样式
- 表单验证（非空检查、密码长度）
- 网络错误友好提示
- 401 未授权自动登出

**JavaScript 核心函数**
- `handleLogin()` - 用户登录
- `handleRegister()` - 用户注册
- `switchSection()` - 板块切换路由
- `loadList()` - 加载音频列表（支持搜索）
- `uploadFile()` - 文件上传（带进度监控）
- `startEdit()` / `saveEdit()` - 音频名称编辑
- [deleteAudio()](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java#L110-L134) - 音频删除
- `loadUserList()` - 加载用户列表
- `showAddUserModal()` / `showEditUserModal()` - 用户模态框
- `saveUser()` - 保存用户（添加/编辑）
- [deleteUser()](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\controller\AuthController.java#L456-L481) - 删除用户
- `loadPendingRegistrations()` - 加载待审核列表
- `showReviewModal()` - 显示审核模态框
- [approveRegistration()](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\controller\AuthController.java#L164-L270) - 提交审核通过
- [rejectRegistration()](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\controller\AuthController.java#L272-L349) - 提交审核拒绝
- `formatFileSize()` - 文件大小格式化
- `handleAudioError()` - 音频加载错误处理

---

### 三、其他贡献

#### 配置文件优化
- [application.yml](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\target\classes\application.yml) 配置审查与维护
- JWT 密钥与过期时间外部化
- 数据库初始化模式调整

#### 文档维护
- [开发日志.md](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\docx\开发日志.md) 持续更新
- [API接口文档.md](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\docx\git\API接口文档.md) 编写
- [plan.md](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\docx\plan.md) 临时计划维护
- 已知 Bug 文档跟踪

#### Bug 修复与稳定性增强
- JWT Claims 解析空指针防护
- 审核人信息查询逻辑优化
- 数据库操作异常处理完善
- `@RequestBody` 参数类型修正
- MyBatis-Plus 驼峰命名规则问题修复
- status 字段全局变量覆盖问题修复

---

## 2. 阚德扬 (kdy) - 后端核心业务开发

### 核心职责概述
负责音频管理核心功能的后端实现，包括文件上传、存储、删除、播放等关键业务逻辑，以及技术优化和日志系统建设。

---

### 一、音频管理核心功能

#### 音频控制器 ([AudioController.java](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\controller\AudioController.java))

**音频上传接口** (`POST /api/audio/upload`)
- 权限验证（管理员 + 操作员）
- 文件格式验证（仅 mp3/wav）
- 文件大小限制（10MB）
- 调用 Service 层完成上传
- 统一异常处理与错误响应

**音频列表分页查询** (`GET /api/audio/list`)
- 支持分页参数（page、size）
- 支持关键词搜索（keyword 可选参数）
- 按创建时间降序排列
- 返回 MyBatis-Plus Page 对象

**音频名称修改接口** (`PUT /api/audio/{id}`)
- 权限验证（仅管理员）
- 文件名非空校验
- 调用 Service 层更新元数据
- 详细的操作日志输出

**音频删除接口** (`DELETE /api/audio/{id}`)
- 权限验证（仅管理员）
- 调用 Service 层执行双重删除
- 物理文件 + 数据库记录同步删除
- 完善的异常捕获与错误提示

**无效记录清理接口** (`POST /api/audio/cleanup`)
- 权限验证（仅管理员）
- 调用 Service 层清理死链记录
- 返回清理数量统计

**音频流式播放接口** (`GET /api/audio/stream/{id}`)
- 免 JWT 拦截（WebConfig 排除）
- 文件存在性验证
- 文件可读性检查
- HTTP 响应头设置
    - Content-Type（audio/mpeg 或 audio/wav）
    - Accept-Ranges: bytes（支持断点续传）
    - Cache-Control（缓存控制）
    - Content-Length（文件大小）
- 返回 FileSystemResource

**音频状态检查接口** (`GET /api/audio/check/{id}`)
- 数据库记录存在性检查
- 物理文件存在性验证
- 返回详细的状态信息

#### 音频服务层 ([AudioService.java](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java))

**文件上传逻辑** ([uploadAudio](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java#L35-L71))
- 文件非空验证
- 文件格式正则匹配（mp3/wav）
- UUID 随机命名防止重名覆盖
- 目录自动创建（`Files.createDirectories`）
- 使用 `Files.copy` 流式拷贝避免内存溢出
- 路径规范化防止穿越攻击（`.normalize()`）
- 从已落盘文件计算 MD5（避免内存占用）
- 事务控制保证原子性（`@Transactional`）
- 异常回滚机制（数据库失败时清除磁盘文件）

**MD5 哈希计算** ([calculateMD5FromFile](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java#L72-L83))
- 从物理文件计算而非内存字节数组
- 防止大文件 OOM
- MessageDigest 标准实现

**分页查询** ([listAudios](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java#L103-L105))
- MyBatis-Plus Page 分页
- 按创建时间降序排列（`orderByDesc`）
- 支持关键词模糊搜索（文件名 + 标签）
- 仅查询正常状态记录（status=1）

**物理+逻辑删除同步** ([deleteAudio](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java#L110-L134))
- 提前锁定物理文件路径
- 先执行数据库逻辑删除
- 再执行物理文件删除
- 检查结果验证
- 文件被占用时的警告日志

**音频名称修改** ([updateAudioName](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java#L138-L146))
- 根据 ID 查询音频资产
- 仅更新 file_name 字段
- 严禁修改物理路径防止死链

**无效记录清理** ([cleanupInvalidRecords](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java#L150-L167))
- 遍历所有正常状态记录
- 检查物理文件是否存在
- 自动删除死链数据库记录
- 返回清理数量统计

**文件存在性验证** ([getAudioFile](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java#L177-L196))
- 根据 ID 查询音频资产
- 验证物理文件是否存在
- 验证文件是否可读
- 详细的错误日志输出

**获取音频资产信息** ([getAudioAssetById](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java#L171-L173))
- 根据 ID 查询完整记录
- 用于状态检查接口

---

### 二、技术优化

#### 文件存储优化
- 使用 `Files.copy` 替代 `MultipartFile.transferTo`
    - 更可靠的文件写入
    - 更好的异常处理
- 路径规范化防止穿越攻击
    - `Paths.get().normalize()`
    - 杜绝 `../` 恶意路径
- 目录自动创建
    - `Files.createDirectories()`
    - 幂等操作，已存在不报错

#### 删除逻辑改进
- 先保存文件路径再逻辑删除
    - 避免删除后无法定位物理文件
- 检查结果验证
    - `audioAssetMapper.deleteById()` 返回值检查
- 异常处理完善
    - try-catch 包裹关键操作
    - 详细的错误日志
    - 友好的错误提示

#### 日志系统
- 详细的操作日志输出
    - `[AudioController]` 前缀标识
    - 请求参数记录
    - 操作结果记录
- 错误追踪信息
    - `System.err.println` 错误输出
    - `e.printStackTrace()` 堆栈打印
    - 便于问题定位

#### Jakarta EE 适配
- 添加 `jakarta.annotation-api` 依赖
- 将 `@Resource` 改为 `@Autowired`
- 修复 Jakarta 与 javax 包名冲突

#### 音频播放优化
- 完善文件播放功能
- 支持多种音频格式（MP3/WAV）
- HTTP 响应头定制
    - Content-Type 识别
    - 缓存控制
    - 断点续传支持

---

### 三、实体类与 Mapper

#### AudioAsset 实体类 ([AudioAsset.java](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\entity\AudioAsset.java))
- 雪花算法 ID 生成（`IdType.ASSIGN_ID`）
- JSON 序列化配置（`@JsonFormat(shape = JsonFormat.Shape.STRING)`）
- 防止前端 Long 精度丢失
- 字段定义
    - id、fileName、filePath
    - fileSize、hashCode
    - createTime、status
    - tags（音频标签）

#### AudioAssetMapper 数据访问层
- 继承 MyBatis-Plus `BaseMapper<AudioAsset>`
- 自动获得 CRUD 方法
- 支持条件构造器查询

---

### 四、辅助功能开发

#### 音频搜索功能增强
- 在 [listAudios](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\service\AudioService.java#L103-L105) 方法添加 keyword 参数
- 支持按文件名模糊匹配
- 支持按标签模糊匹配
- 使用 MyBatis-Plus 的 `like` 条件构建
- 保持原有的分页和排序逻辑

#### AudioAsset 实体扩展
- 添加 [tags](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\entity\AudioAsset.java#L22-L22) 字段支持音频标签分类
- 数据库表结构同步更新

---

## 3. 杨硕 (ysh) - 后端开发（实体与数据库）

### 核心职责概述
负责项目基础设施搭建、数据库设计与实现、实体类开发以及 MyBatis-Plus 框架配置，专注于数据层和持久层的后端开发工作。

---

### 一、数据库设计与实现

#### 数据库表结构设计

**audio_assets 表（音频资产表）**
```sql
CREATE TABLE `audio_assets` (
  [id](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\entity\AudioAsset.java#L12-L14) BIGINT PRIMARY KEY,
  `file_name` VARCHAR(255) NOT NULL,
  `file_path` VARCHAR(500) NOT NULL,
  `file_size` BIGINT DEFAULT 0,
  `hash_code` VARCHAR(64),
  `create_time` DATETIME NOT NULL,
  [status](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\entity\AudioAsset.java#L21-L21) INT DEFAULT 1,
  [tags](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\entity\AudioAsset.java#L22-L22) VARCHAR(500) COMMENT '标签，多个标签用逗号分隔',
  PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='音频资产表';