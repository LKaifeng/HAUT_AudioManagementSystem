# 开发日志

## 项目开发时间线

---

### 第一阶段：项目初始化与基础架构搭建（于子桐(kaifeng)）

#### Commit: ac43ffe - 实体类与Mapper基础

- **负责人**: 杨硕 (ysh)
- **工作内容**:
    - 创建 AudioAsset 实体类，配置 MyBatis Plus 注解
    - 使用雪花算法生成长整型 ID
    - 添加 `@JsonFormat(shape = JsonFormat.Shape.STRING)` 防止前端精度丢失
    - 创建 AudioAssetMapper 接口继承 BaseMapper
- **技术要点**: ID 精度保护、ORM 框架选型

---

#### Commit: d35832a - 控制器与服务层框架

- **负责人**: 阚德扬 (kdy)
- **工作内容**:
    - 创建 AudioController 空框架
    - 创建 AudioService 空框架
    - 完善 AudioAssetMapper 实现
- **技术要点**: 分层架构设计

---

#### Commit: 7f23e38 - 依赖配置与应用配置

- **负责人**: 杨硕 (ysh)
- **工作内容**:
    - 修正 Spring Boot 版本为 3.2.5（稳定版）
    - 添加 MyBatis-Plus 3.5.7 依赖
    - 添加 JWT 依赖（jjwt 0.11.5）
    - 配置 `application.yml`：数据源、文件上传限制、存储路径
    - 配置 MyBatis-Plus：驼峰映射、SQL 日志、逻辑删除
- **技术要点**: Spring Boot 3 兼容性、依赖版本管理

---

### 第二阶段：核心业务功能实现

#### Commit: 061686b - 音频管理完整功能实现

- **负责人**: 阚德扬 (kdy)
- **工作内容**:
    - 实现音频上传接口（UUID 命名、物理文件保存）
    - 实现分页查询接口（按创建时间降序）
    - 实现删除接口（物理文件 + 数据库记录同步删除）
    - 实现音频流式播放接口
    - 添加跨域支持 `@CrossOrigin`
- **技术要点**: 事务控制、文件 IO 操作、RESTful API 设计

---

#### Commit: 8cd4951 - 文件格式验证与MD5优化

- **负责人**: 阚德扬 (kdy)
- **工作内容**:
    - 添加文件格式验证（仅支持 mp3/wav）
    - 实现 MD5 哈希计算并存储到数据库
    - 优化文件保存方式：使用 `transferTo` 避免内存溢出
    - 调整删除逻辑顺序：先逻辑删除再物理删除
- **技术要点**: 文件安全校验、哈希算法应用

---

#### Commit: f8e2a41 - 文件保存问题修复

- **负责人**: 阚德扬 (kdy)
- **工作内容**:
    - 使用 `Files.copy` 替代 `MultipartFile.transferTo`
    - 从已保存的物理文件计算 MD5，而非内存中的字节数组
    - 改进异常处理机制，抛出运行时异常
    - 增强路径规范化和目录创建的安全性
- **技术要点**: 大文件处理优化、安全性增强

---

### 第三阶段：安全认证体系构建

#### Commit: 59cee84 - JWT认证模块开发

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - 创建 SysUser 实体类（用户名、密码、角色等级）
    - 实现 JwtUtil 工具类（Token 生成与解析）
    - 开发 JwtInterceptor 拦截器（Token 验证）
    - 创建 AuthController 提供登录接口
    - 在 `schema.sql` 中添加 `sys_users` 表和初始测试账户
- **技术要点**: JWT 鉴权、角色权限模型、拦截器模式

---

#### Commit: 1621828 - JWT拦截器配置与测试数据

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - 创建 WebConfig 配置类注册 JWT 拦截器
    - 配置拦截路径 `/api/**`，排除登录接口
    - 在 `schema.sql` 中添加管理员和操作员测试账户
        - 管理员：admin/123456, manager/admin123
        - 操作员：operator1/user123, operator2/test123, guest/guest123
- **技术要点**: Spring MVC 拦截器链配置

---

#### Commit: 6dc46b6 - 权限控制实现与问题修复

- **负责人**: 于子桐 (kaifeng) & 阚德扬 (kdy)
- **工作内容**:
    - **于子桐**:
        - 修改 JwtInterceptor 提取 roleLevel 并注入请求属性
        - 更新 `plan.md` 文档，标记已解决问题
    - **阚德扬**:
        - 在 AudioController 上传接口添加权限验证（管理员+操作员）
        - 在删除接口添加权限验证（仅管理员）
- **技术要点**: 基于角色的访问控制（RBAC）

---

#### Commit: 101e846 - JWT密钥外部化配置

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - 将 JWT 密钥从硬编码改为从 `application.yml` 读取
    - 使用 `@Value` 注解注入 secret 和 expiration
    - 修复签名密钥获取方式，使用标准字符集编码
    - 在 `application.yml` 中配置 JWT 密钥和 24 小时过期时间
- **技术要点**: 配置外部化、安全性提升

---

### 第四阶段：前端界面开发

#### Commit: 8b94452 - 前端基础页面

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - 创建基础 HTML 页面（`index.html`）
    - 实现上传模块（文件选择、上传按钮）
    - 实现音频列表展示（表格形式）
    - 实现音频播放控件
    - 实现删除功能
    - 编写 JavaScript 交互逻辑
- **技术要点**: HTML5 Audio API、Fetch API、DOM 操作

---

#### Commit: 9c6fb1e - 前端登录认证与权限控制

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - 实现用户登录界面
    - 添加本地存储管理 token 和角色权限
    - 实现页面访问控制（未登录无法访问主功能）
    - 增加管理员权限验证（仅管理员可删除）
    - 更新 API 接口路径并添加认证头信息
    - 调整页面布局（登录区与主功能区分离）
- **技术要点**: LocalStorage、Token 认证、条件渲染

---

#### Commit: 050bcd1 - 前端分页与体验优化

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - 添加分页控件（上一页/下一页）
    - 实现前端分页逻辑（每页 10 条记录）
    - 优化登录验证（用户名密码非空检查）
    - 改进网络错误提示信息
    - 添加上传文件类型和大小验证（仅 mp3/wav，最大 10MB）
    - 增加禁用按钮样式
    - 优化删除确认提示
    - 改进错误处理（401 未授权状态处理）
    - 优化上传状态显示
    - 添加无数据提示和加载失败提示
    - 调整 API 基础路径从 localhost 改为相对路径
- **技术要点**: 分页算法、表单验证、用户体验优化

---

### 第五阶段：功能增强与优化

#### Commit: b3a8a05 - Jakarta注解适配

- **负责人**: 阚德扬 (kdy)
- **工作内容**:
    - 添加 `jakarta.annotation-api` 依赖
    - 将 `@Resource` 改为 `@Autowired`（AudioController）
    - 修复 Jakarta 与 javax 包名冲突问题
- **技术要点**: Spring Boot 3 Jakarta EE 迁移

---

#### Commit: 6789a74 - 音频名称修改与播放优化

- **负责人**: 阚德扬 (kdy)
- **工作内容**:
    - 新增 PUT 接口用于修改音频名称（仅管理员）
    - 增加权限验证机制
    - 实现音频名称验证（非空检查）
    - 完善文件播放功能，支持多种音频格式（MP3/WAV）
    - 添加 HTTP 响应头设置（内容类型、缓存控制等）
    - 优化服务层 `updateAudioName` 方法
    - 改进 `getAudioFile` 方法，增加文件存在性验证
    - 修复文件路径处理，使用绝对路径
- **技术要点**: RESTful PUT 方法、HTTP 头部定制、ContentType 识别

---

#### Commit: 53f2e9d - MyBatis-Plus版本修复

- **负责人**: 杨硕 (ysh)
- **工作内容**:
    - 升级 MyBatis-Plus 版本从 3.5.5 到 3.5.7
    - 修复 Spring Boot 3 兼容性问题
    - 添加 `@NonNull` 注解到 JwtInterceptor
    - 移除不必要的导入
- **技术要点**: 版本兼容性修复

---

#### Commit: d3c66cf - Spring Boot 3专用Starter

- **负责人**: 杨硕 (ysh)
- **工作内容**:
    - 使用 `mybatis-plus-spring-boot3-starter` 替代通用 starter
    - 强制覆盖 mybatis-spring 版本为 3.0.3
    - 确保 Spring Boot 3 完全兼容
- **技术要点**: Spring Boot 3 生态适配

---

### 第六阶段：高级功能与系统完善

#### Commit: 39c2f9c - 用户管理与音频清理功能

- **负责人**: 于子桐 (kaifeng) & 阚德扬 (kdy)
- **工作内容**:
    - **于子桐 (AuthController)**:
        - 实现用户注册接口
        - 添加获取用户列表接口（仅管理员）
        - 实现添加用户功能
        - 添加更新用户信息功能（修改密码和角色）
        - 实现删除用户功能
        - 统一 API 响应格式（code + msg）
    - **阚德扬 (AudioController)**:
        - 将 update 和 delete 方法的参数类型从 Long 改为 String
        - 添加详细的系统日志输出
        - 实现音频流服务的多重检查机制
        - 新增 cleanup 接口用于清理无效记录
        - 添加音频文件状态检查接口
        - 改进错误处理逻辑
- **技术要点**: 完整的 CRUD、日志系统、数据一致性维护

---

#### Commit: a2c19f3 - 音频服务功能完善

- **负责人**: 阚德扬 (kdy)
- **工作内容**:
    - 添加音频流接口路径到 JWT 拦截器排除列表
    - 改进 `deleteAudio` 方法：
        - 添加异常处理
        - 保存文件路径后再逻辑删除
        - 检查删除结果
    - 新增 `cleanupInvalidRecords` 方法清理无效记录
    - 优化 `getAudioFile` 方法增加详细的错误日志
    - 添加 `getAudioAssetById` 方法获取音频资产信息
- **技术要点**: 健壮性增强、数据清理机制

---

#### Commit: 7871a25 - 合并分支与最终优化

- **负责人**: 团队协作
- **工作内容**:
    - 合并各分支代码
    - 统一代码风格
    - 最终测试验证
- **技术要点**: 版本控制协作

---

### 第七阶段：UI重构与用户体验升级

#### Commit: e682316 / f96ef99 - 前端UI全面重构

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - **全新 UI 设计**:
        - 现代化侧边栏导航布局
        - 渐变背景与卡片式设计
        - 三大功能板块：音频乐库、上传音频、用户管理
    - **交互优化**:
        - 拖拽上传支持
        - 实时上传进度条
        - 文件信息动态展示
        - 平滑的板块切换动画
    - **功能增强**:
        - 音频编辑功能（行内编辑）
        - 文件大小格式化显示
        - 音频加载错误详细诊断
        - 空状态友好提示
    - **用户管理完善**:
        - 模拟用户数据展示
        - 角色徽章区分（管理员/操作员）
        - 用户头像自动生成
    - **配置调整**:
        - 修改 `sql.init.mode` 为 `never`（不自动初始化）
- **技术要点**: CSS Grid/Flexbox、动画效果、拖拽 API、XMLHttpRequest 进度监听

---

#### Commit: e063c32 - 用户管理功能完整实现

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - 实现用户增删改查完整功能
    - 添加/编辑用户模态框
    - 增加角色权限控制（管理员 vs 操作员）
    - 添加用户列表展示界面和相关样式
    - 修复文件管理中 ID 精度丢失问题（ID 转字符串）
    - 优化音频加载错误处理和用户反馈
    - 调整前端样式细节
- **技术要点**: 模态框交互、表单验证、逆向数据控制

---

### 第八阶段：用户注册审核系统开发

#### Commit: fa72664 - 用户注册实体与数据访问层

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - 创建 [UserRegistration](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\entity\UserRegistration.java#L8-L20) 实体类
    - 包含字段：用户名、密码、申请时间、状态、审核评论、审核人 ID、审核时间
    - 使用 MyBatis-Plus 注解配置表映射关系
    - 创建 [UserRegistrationMapper](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\mapper\UserRegistrationMapper.java#L6-L8) 接口继承 `BaseMapper`
    - 提供基本的 CRUD 操作支持
- **技术要点**: 实体类设计、ORM 映射、自动主键生成

---

#### Commit: b57380e / a21aaee - 注册功能基础设施搭建

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - 在 [WebConfig](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\config\WebConfig.java#L8-L23) 中为 `/api/auth/register` 添加拦截器排除规则
    - 允许未登录用户访问注册接口
    - 在 [AudioAsset](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\entity\AudioAsset.java#L9-L23) 实体中添加 [tags](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\entity\AudioAsset.java#L22-L22) 字段支持音频标签分类
    - 在数据库中创建 `user_registration` 表
    - 设置唯一索引防止重复用户名申请
    - 定义审核状态枚举：0-待审核、1-已通过、2-已拒绝
- **技术要点**: 安全路径配置、数据库表设计、唯一性约束

---

#### Commit: 0c8fe36 - 注册审核核心功能实现

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - **注册提交接口 (`POST /api/auth/register`)**:
        - 验证用户名和密码非空
        - 检查用户名是否已存在于正式用户表
        - 检查是否存在待审核的相同用户名申请
        - 创建注册申请记录并设置状态为待审核
        - 返回提示信息"注册申请已提交，请等待管理员审核"
    
    - **获取待审核列表接口 (`GET /api/auth/registrations`)**:
        - 仅允许管理员（roleLevel=0）访问
        - 支持分页查询待审核申请（status=0）
        - 按申请时间降序排列
        - 返回详细的分页信息（总数、页数、当前页等）
        - 添加详细的调试日志输出
    
    - **审核通过接口 (`POST /api/auth/registrations/{id}/approve`)**:
        - 权限验证：仅管理员可操作
        - 检查申请是否存在且处于待审核状态
        - 再次验证用户名未被占用（防止并发冲突）
        - 创建新用户并分配默认角色（操作员 roleLevel=1）
        - 更新申请状态为已通过
        - 记录审核人 ID 和审核时间
        - 支持可选的审核备注信息
    
    - **审核拒绝接口 (`POST /api/auth/registrations/{id}/reject`)**:
        - 权限验证：仅管理员可操作
        - 检查申请是否存在且处于待审核状态
        - 更新申请状态为已拒绝
        - 记录审核人 ID、审核时间和拒绝原因
        - 不创建新用户账号
    
    - **音频搜索功能增强**:
        - 在 [AudioController.list()](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\controller\AudioController.java#L45-L51) 接口添加 `keyword` 参数
        - 支持按文件名或标签进行模糊搜索
        - 使用 MyBatis-Plus 的 `like` 条件构建查询
    
    - **密码加密工具类 ([PasswordUtil](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\util\PasswordUtil.java#L7-L24))**:
        - 实现 SHA-256 哈希算法
        - 提供密码加密和验证方法
        - 使用 Base64 编码存储哈希值
        - *注：由于集成问题暂时注释未启用*
    
    - **配置调整**:
        - 注释掉 MyBatis-Plus 全局逻辑删除配置
        - 避免与注册审核状态字段冲突
- **技术要点**: 审核流程设计、并发控制、事务管理、模糊搜索

---

#### Commit: 68f37b9 - 注册审核 Bug 修复与稳定性增强

- **负责人**: 于子桐 (kaifeng)
- **工作内容**:
    - **修复 JWT Claims 解析空指针问题**:
        - 优化从 Request 属性中提取 Claims 的逻辑
        - 先转换为 Claims 对象再获取用户名
        - 避免链式调用导致的潜在 NPE
    
    - **修复审核人信息查询逻辑**:
        - 增加用户名空值检查（`username != null && !username.isEmpty()`）
        - 当 Token 中无用户名时给出明确警告日志
        - 区分"找不到审核人"和"Token 未包含用户名"两种情况
    
    - **添加数据库操作异常处理**:
        - 在 `userMapper.insert(newUser)` 周围添加 try-catch
        - 在 `registrationMapper.updateById(registration)` 周围添加 try-catch
        - 捕获异常后返回友好的错误信息和 500 状态码
        - 打印详细堆栈信息便于调试
    
    - **修复 `@RequestBody` 参数类型错误**:
        - 将 [approveRegistration](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\controller\AuthController.java#L164-L270) 方法的 `required = null` 改为 `required = false`
        - 将 [rejectRegistration](file://c:\Users\KaiFeng\Desktop\HAUT_AudioManagementSystem\src\main\java\com\example\haut_audiomanagementsystem\controller\AuthController.java#L272-L349) 方法的 `@RequestBody` 也设置为 `required = false`
        - 允许审核接口不传递备注信息
    
    - **更新已知 Bug 文档**:
        - 标记所有注册审核相关 Bug 为已修复状态
        - 包括：驼峰命名问题、status 覆盖问题、userID 缺失问题
- **技术要点**: 防御性编程、异常处理、空指针防护、代码健壮性

---

## 开发统计总结

|阶段|时间跨度|主要成果|Commit 数量|
|---|---|---|---|
|第一阶段|项目启动|基础架构搭建|3|
|第二阶段|核心开发|音频 CRUD 功能|3|
|第三阶段|安全加固|JWT 认证体系|4|
|第四阶段|前端开发|基础 UI 与交互|3|
|第五阶段|功能增强|编辑、优化、适配|4|
|第六阶段|系统完善|用户管理、清理|2|
|第七阶段|UI 重构|现代化界面升级|2|
|第八阶段|注册审核|用户注册审核系统|4|

**总计**: 42个 Commits

---

## 技术债务与后续优化建议

1. **批量删除**: 支持前端多选音频，后端批量物理与逻辑删除。
2. **音频评论**：支持用户针对单个音频发表与查看评论。
3. **点赞与收藏**：支持用户点赞或收藏音频，并记录关联关系。
4. **榜单排序**：支持按点赞量或收藏量升降序展示音频列表。
5. **音频评论**：支持用户针对单个音频发表与查看评论。
6. **互动界面适配**：前端新增评论区及点赞收藏状态的 UI 展示面板。
7. **响应格式统一**：封装全局通用响应体，规范化 API 返回数据。
